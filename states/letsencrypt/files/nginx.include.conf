{#
Include this in your nginx 80 server config for servers you wish
to use lets encrypt for SSL certificate authentication.

e.g:

    include /etc/letsencrypt/nginx.include.conf;

#}
# Lets Encrypt Web Root Path for ACME Challenge
location /.well-known/acme-challenge {
    root /var/www/letsencrypt;
}
